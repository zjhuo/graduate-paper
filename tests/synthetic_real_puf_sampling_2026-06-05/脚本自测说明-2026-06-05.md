# analyze_real_puf_sampling.py synthetic 数据自测说明（2026-06-05）

## 1. 自测目的
本次自测用于证明：
- `analyze_real_puf_sampling.py` 不仅能启动；
- 核心统计口径（uniformity、reliability、raw BER、uniqueness、bit-aliasing、aggregate BER）在一份可手工验证的 synthetic 数据上能得到正确结果。

本次数据为人工构造的脚本自测数据，不是真实 PUF 实测结果。

## 2. synthetic 数据设置
- 板数：2 块（`board_001`、`board_002`）
- 共同 challenge 数：4 个（`challenge_index = 0,1,2,3`）
- 每个 challenge 重复采样次数：5 次
- `selection_mode`：`global_shared`
- 特意加入：
  - 少量比特翻转；
  - 两条 `raw_resp_valid = 0` 的无效样本

说明：脚本统计时只使用 `raw_resp_valid = 1` 的有效样本，因此无效样本应被排除。

## 3. 手工预期值
### board_001
- uniformity（均衡性） = 9 / 19 = **0.473684**
- reliability（稳定性） = mean(0.8, 1.0, 1.0, 0.8) = **0.900000**
- raw BER（原始误码率） = mean(0.2, 0.0, 0.0, 0.2) = **0.100000**
- aggregate BER（聚合后误码率） = 0 / 4 = **0.000000**

### board_002
- uniformity（均衡性） = 9 / 19 = **0.473684**
- reliability（稳定性） = mean(0.8, 1.0, 0.8, 0.8) = **0.850000**
- raw BER（原始误码率） = mean(0.8, 0.0, 0.2, 0.8) = **0.450000**
- aggregate BER（聚合后误码率） = 2 / 4 = **0.500000**

### 多板共同 challenge 指标
- uniqueness（共同 challenge set 下的 inter-chip Hamming distance）
  - 板 1 聚合位串：`[1,0,1,0]`
  - 板 2 聚合位串：`[0,0,1,1]`
  - 差异位数：2 / 4 = **0.500000**

- bit-aliasing（同一 challenge 在多板上为 1 的比例）
  - `idx:0` = (1,0) -> **0.500000**
  - `idx:1` = (0,0) -> **0.000000**
  - `idx:2` = (1,1) -> **1.000000**
  - `idx:3` = (0,1) -> **0.500000**

## 4. 脚本实际输出
### board_metrics.csv
- `board_001,0.473684,0.900000,0.100000,0.000000`
- `board_002,0.473684,0.850000,0.450000,0.500000`

### pairwise_distance.csv
- `board_001,board_002,4,0.500000`

### bit_aliasing.csv
- `idx:0,0.500000`
- `idx:1,0.000000`
- `idx:2,1.000000`
- `idx:3,0.500000`

## 5. 对比结论
| 指标 | 预期值 | 脚本输出 | 是否一致 |
|---|---:|---:|---|
| board_001 uniformity | 0.473684 | 0.473684 | 是 |
| board_001 reliability | 0.900000 | 0.900000 | 是 |
| board_001 raw BER | 0.100000 | 0.100000 | 是 |
| board_001 aggregate BER | 0.000000 | 0.000000 | 是 |
| board_002 uniformity | 0.473684 | 0.473684 | 是 |
| board_002 reliability | 0.850000 | 0.850000 | 是 |
| board_002 raw BER | 0.450000 | 0.450000 | 是 |
| board_002 aggregate BER | 0.500000 | 0.500000 | 是 |
| uniqueness | 0.500000 | 0.500000 | 是 |
| bit-aliasing idx:0 | 0.500000 | 0.500000 | 是 |
| bit-aliasing idx:1 | 0.000000 | 0.000000 | 是 |
| bit-aliasing idx:2 | 1.000000 | 1.000000 | 是 |
| bit-aliasing idx:3 | 0.500000 | 0.500000 | 是 |

## 6. 当前结论
本次 synthetic 数据自测表明：
- `analyze_real_puf_sampling.py` 能完整运行；
- 关键统计指标的计算口径在这组可手工核验的数据上是正确的；
- `raw_resp_valid = 0` 的无效样本能够被正确排除；
- `global_shared` 模式下，脚本会把 uniqueness 按共同 challenge set 的 inter-chip Hamming distance 处理。

再次强调：
> 本次结果仅用于脚本自测，不代表真实 PUF 实测结论，也不能替代后续真实板级采样验证。
